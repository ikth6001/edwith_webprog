<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ikth.apps.reserve.product.dao.ProductDao">

	<select id="selectProductsByCode"
		resultType="com.ikth.apps.reserve.product.vo.ProductVO">
		SELECT NAME as name
				, description as description
				, place as place
				, typeCd as typeCd
				, promotion as promotion
				, TO_BASE64(img) as imgBase64
		FROM product
		WHERE typeCd=#{code}
		ORDER BY name
		LIMIT 0, #{cnt}
	</select>

	<select id="selectProducts"
		resultType="com.ikth.apps.reserve.product.vo.ProductVO">
		SELECT NAME as name
				, description as description
				, place as place
				, typeCd as typeCd
				, promotion as promotion
				, TO_BASE64(img) as imgBase64
		FROM product
		ORDER BY name
		LIMIT 0, #{cnt}
	</select>
	
	<select id="selectPromotionedProducts"
		resultType="com.ikth.apps.reserve.product.vo.ProductVO">
		SELECT NAME as name
				, description as description
				, place as place
				, typeCd as typeCd
				, promotion as promotion
				, TO_BASE64(img) as imgBase64
		FROM product
		WHERE promotion=true
	</select>
	
	<select id="selectCountProducts"
		resultType="int">
		SELECT COUNT(*)
		FROM product
		<if test="code != null">
			WHERE typeCd=#{code}
		</if>
	</select>
	
</mapper>